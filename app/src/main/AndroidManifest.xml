<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="hr.fer.ruazosa.taskpaper">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TaskPaper">
        <activity
            android:name=".TaskDetailsDropboxActivity"
            android:exported="false" />
        <activity
            android:name=".TaskDetailsActivity"
            android:exported="false" />
        <activity
            android:name=".DropboxLoginActivity"
            android:exported="false" />
        <activity
            android:name=".DropboxHomeActivity"
            android:exported="false" />
        <activity
            android:name=".HomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>